export const styles = theme => ({
    copyrighttext: {
        background: theme.palette.primary.main,
        padding: "16px 0px 16px 0px",
        "& p": {
            margin: 0,
            textAlign: "center",
            color: "white",
        }
    }
});